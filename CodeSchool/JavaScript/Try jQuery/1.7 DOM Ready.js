/*1.7 DOM Ready
Great! Now we just have to make sure the code runs after the page has finished loading (when the DOM is ready), go ahead and do that now.*/

$(document).ready(function(){
	$("span").text("$100");
});